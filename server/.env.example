TMDB_API_KEY=
REMOVEBG_API_KEY=
ANTHROPIC_API_KEY=
REPLICATE_API_KEY=
PORT=4310

# Optional: when AUTH_PASSWORD is set, every request is gated behind a single shared HTTP Basic
# Auth login (browser will prompt). Leave both unset for no login (local dev default).
AUTH_USERNAME=team
AUTH_PASSWORD=
